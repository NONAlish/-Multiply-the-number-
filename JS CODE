function multiply(number) {
  return number < 0 ? -(Math.abs(number) * (5 ** Math.max(1, Math.abs(number).toString().length))) : (Math.abs(number) * (5 ** Math.max(1, Math.abs(number).toString().length)));
}
